# Rename this file to .env at the project root

# Backend service (NestJS)
# - BACKEND_PORT: Port NestJS listens on
# - FRONTEND_URL: Allowed origin for CORS (your Next.js URL)
BACKEND_PORT=3001
FRONTEND_URL=http://localhost:3000

# Frontend app (Next.js)
# - NEXT_PUBLIC_BACKEND_URL: Base URL the frontend uses to call the backend
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
